The Netlify deploy errored, with the following guidance provided:

Diagnosis
- Relevant lines: the Yarn warning about mixed lockfiles is at [line 21](#L21), the failing error is at [line 23](#L23) ("Can't add "@types/jest": invalid package version undefined."), and Yarn exit is at [line 24](#L24).
- Error type and cause: Yarn v1 fails during install because your dependency entry for @types/jest is malformed (its version is undefined) or the repository contains mixed lockfiles (package-lock.json + yarn.lock) causing Yarn to choke when resolving that dependency.

What to check first
1. Verify @types/jest is present and has a valid version string in package.json: https://github.com/xxbattousaixx/solafide3/blob/main/package.json
   - Make sure the entry looks like "@types/jest": "^29.5.0" (a string), not null/undefined or an object.
2. Verify you committed the corresponding lockfile you intend to use:
   - If you want to use Yarn, commit yarn.lock and remove package-lock.json.
   - If you want to use npm, commit package-lock.json and remove yarn.lock.

Fix options (pick one)

A — Keep Yarn (recommended if you already have yarn.lock)
1. Fix or add @types/jest in package.json (locally):
   - With Yarn:
```bash
yarn add --dev @types/jest@latest
```
   - This will update package.json and yarn.lock with a valid version string.
2. Remove the npm lockfile (to avoid the mixed-lockfile warning) and commit:
```bash
git rm package-lock.json
git add package.json yarn.lock
git commit -m "fix: add @types/jest version and remove package-lock.json"
git push
```
3. Redeploy on Netlify. If you want, clear the Netlify build cache and trigger a fresh deploy.

B — Switch to npm (if you prefer npm)
1. Remove yarn.lock locally:
```bash
git rm yarn.lock
```
2. Install with npm so package-lock.json is consistent:
```bash
npm install --save-dev @types/jest@latest
git add package.json package-lock.json
git commit -m "fix: add @types/jest and use npm lockfile"
git push
```
3. Redeploy on Netlify.

Additional notes
- Make sure the updated package.json and the chosen lockfile are committed to the repo before deploying; Netlify detected Yarn because yarn.lock exists — it will use Yarn automatically.
- After fixing and committing, redeploy. If you still see errors, re-open package.json and check for any dependency entries with non-string versions (null/undefined) and fix them similarly.

If you see any Node version mismatch after this step, follow Netlify’s docs to change Node versions: https://docs.netlify.com/configure-builds/manage-dependencies/#node-js-and-javascript

The relevant error logs are:

Line 0: build-image version: ac6eb13fbf000e5c09ad677efd8b7c3c2d0142b6 (noble-new-builds)
Line 1: buildbot version: 9cf689c010ee4ab6135ad4a5e6b06ccb513df1ed
Line 2: Fetching cached dependencies
Line 3: Failed to fetch cache, continuing with build
Line 4: Starting to prepare the repo for build
Line 5: No cached dependencies found. Cloning fresh repo
Line 6: git clone --filter=blob:none https://github.com/xxbattousaixx/solafide3
Line 7: Preparing Git Reference refs/heads/main
Line 8: Custom build command detected. Proceeding with the specified command: 'npm run build'
Line 9: Installing dependencies
Line 10: mise ~/.config/mise/config.toml tools: python@3.14.3
Line 11: mise ~/.config/mise/config.toml tools: ruby@3.4.8
Line 12: mise ~/.config/mise/config.toml tools: go@1.26.2
Line 13: Attempting Node.js version '20' from .nvmrc
Line 14: v20.20.2 is already installed.
Line 15: Now using node v20.20.2 (npm v10.8.2)
Line 16: Enabling Node.js Corepack
Line 17: No yarn workspaces detected
Line 18: Installing npm packages using Yarn version 1.22.22
Line 19: Failed during stage 'Install dependencies': dependency_installation script returned non-zero exit code: 1
Line 20: yarn install v1.22.22
Line 21: warning package-lock.json found. Your project contains lock files generated by tools other than Yarn. It is advised not to mix p
Line 22: [1/4] Resolving packages...
Line 23: error Can't add "@types/jest": invalid package version undefined.
info Visit https://yarnpkg.com/en/docs/cli/install for documen
Line 24: Error during Yarn install
Line 25: Failing build: Failed to install dependencies
Line 26: Finished processing build request in 6.357s